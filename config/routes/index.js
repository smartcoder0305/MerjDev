const footer = require('./footer');
const nav = require('./nav');
const profileNav = require('./profileNav');
const mobileNav = require('./mobileNav');
module.exports = {
  footer,
  nav,
  profileNav,
  mobileNav
};
