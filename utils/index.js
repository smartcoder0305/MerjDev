import dom from './dom';
import getRouteMeta from './getRouteMeta';
import time from './time';

const utils = {
  dom,
  getRouteMeta,
  time,
};

export { dom, getRouteMeta, time };

export default utils;
